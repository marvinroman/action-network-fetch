version: "3.9"

services:

  fetch:
    build: 
      context: ./
    container_name: iedsa_fetch
    env_file:
      - ./.env
    volumes: 
      - "./dist:/dist"
      - "./src:/src"
    command: ["/usr/bin/tail", "-f", "/dev/null"]